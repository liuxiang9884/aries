# AGENTS

本文件定义 `aries` 项目的项目级代理协作约定。进入本仓库工作时，默认先读取并遵守这里的规则。

## 默认约定

- 默认使用中文回答，除非用户明确要求其他语言。
- 文档（`docs/`、`README.md`、设计/审查/计划等 markdown）使用中文撰写；代码注释使用英文，除非相邻代码已有不同约定。提交信息（commit message）使用英文。
- 文档中的描述性语言默认使用中文；专业名词、数据字段、枚举值、配置项、命令行参数、文件路径、代码标识符、日志原文、论文 / 数据供应商 / 外部 API 名称保留英文或原文。
- 优先遵循仓库现有语言、构建脚本、依赖管理方式、目录结构和代码风格；在项目结构尚未明确前，不主动引入大型框架或与当前任务无关的重构。
- `aries` 是日内策略研究仓库，重点覆盖数据处理与存储、特征 / 因子计算与验证、回测框架、模型构造、研究报告和实验复现。默认同时关注数据正确性、时间一致性、可复现性、统计可靠性、计算效率和结果可解释性。
- 对收益、回撤、Sharpe、IC、IR、hit rate、turnover、capacity、延迟、吞吐、存储效率或模型效果相关结论，必须基于实际回测、样本外验证、ablation、统计检验、profile、benchmark 或可复现实验，不凭主观判断宣称完成。
- 代理自行创建的 log、scratch config、临时输出、实验临时产物和大体积中间结果默认写入 `~/tmp`（当前机器为 `/home/liuxiang/tmp`）或仓库既有 artifacts / build / cache 目录；除非用户明确要求或工具强制，不要把新的临时文件散落在仓库根目录，也不要把大数据文件、模型权重或实验缓存纳入 git。

## 对话生命周期和 onboarding

### 新对话启动

每个新对话进入本仓库后，先执行：

```bash
git status --short --branch
git log --oneline -8
```

然后按顺序读取：

```text
AGENTS.md
README.md
docs/project_onboarding_guide.md
docs/research_workflow.md
docs/data.md
docs/factors.md
docs/backtesting.md
docs/models.md
docs/testing.md
```

如果某些文档不存在，先读取项目中最接近的 onboarding、README、研究流程、数据说明、回测说明、模型说明和测试说明。读取后以当前代码、文档、`git status` 和 `git log` 为事实源，不假设本地知识是最新的。

### Onboarding 文档书写规则

`docs/project_onboarding_guide.md` 是新对话接手入口，不是完整历史记录或设计说明。更新 onboarding 时默认遵守：

- 只写当前事实、关键入口、重要边界、验证命令、实验复现方式和下一步建议。
- 细节放到对应专题文档：数据源 / schema / 存储放数据文档，因子定义和验证放因子文档，回测假设和撮合 / 成本模型放回测文档，模型结构和训练评估放模型文档。
- 已完成内容按模块合并，避免逐轮追加历史流水账、完整 benchmark 输出、完整回测日志或已废弃方案。
- 研究结论必须记录样本区间、频率、资产池、过滤条件、成本模型、训练 / 验证 / 测试切分、随机种子和关键指标；无法复现的结论不要写成事实。
- 下一步建议必须可执行，具体到模块、文件、数据集、实验脚本或验证入口。

### 结束对话流程

当用户输入“结束对话”，或明确要求结束当前对话并交接时，默认执行：

1. 运行 `git status --short --branch` 和 `git log --oneline -8`。
2. 对照当前实现、实验产物和最近提交，更新相关文档，重点同步 `docs/project_onboarding_guide.md` 中的当前状态、代码入口、验证命令、实验结果边界和下一步建议。
3. 如果本轮改动影响数据 schema、因子定义、回测假设、成本模型、训练评估流程或指标口径，同步更新对应专题文档。
4. 至少运行 `git diff --check`；如果本轮修改了代码、实验脚本或数据处理逻辑，运行对应最小测试、回测 smoke、数据一致性检查或复现实验。
5. 整理当前状态、已完成事项、验证命令、未完成风险和下一步建议。
6. 是否提交按用户请求和当前任务约定执行；提交前不要裹带无关改动。除非用户明确要求，不主动 push。

## 项目背景

`aries` 面向日内策略研究和验证，当前职责包括但不限于：

- 原始数据获取、清洗、标准化、校验、存储和版本管理。
- 日内 bar、tick、order book、成交、盘口衍生数据等研究数据集构建。
- 因子 / 特征计算、去极值、标准化、中性化、滞后处理、覆盖率和稳定性验证。
- 因子有效性验证，包括 IC / RankIC、分组收益、decay、turnover、相关性、冗余度和样本外表现。
- 回测框架，包括信号对齐、持仓生成、成交假设、手续费 / 滑点 / 冲击成本、资金约束、风险控制和绩效归因。
- 模型构造，包括特征工程、标签定义、训练 / 验证 / 测试切分、交叉验证、调参、校准、解释和部署前评估。
- 研究报告、实验配置、指标口径和复现脚本。

协作代理工作时应优先关注数据链路边界、时间戳语义、样本切分、look-ahead bias、survivorship bias、重复样本、缺失值处理、成本模型、交易约束、统计显著性和可复现证据。

## 多轮 Review 触发词

当用户输入“多轮review”“多轮 review”“multi-round review”或等价表达时，默认先询问用户本次 review 的目标，不自行假设目标。用户给出目标后，按下面流程执行：

1. 明确 review 范围、目标和停止条件；如果目标是“代码逻辑没有问题”，重点检查控制流、数据对齐、时间切分、指标口径、错误处理、资源使用、实验可复现性和统计假设，不把单纯风格偏好作为阻断问题。
2. 每一轮至少派一个 subagent 做只读 review；如果当前工具不支持 subagent，先说明限制，再用主会话执行等价的只读 review。
3. 每轮 review 结束后，先向用户输出本轮发现的问题，按 Critical / Important / Minor 分组，并尽量给出文件路径、行号和影响说明。
4. 根据 review 结果执行修复：Critical 必须修，Important 默认修；Minor 只有在影响既定目标、风险低或用户要求时修。修复时保持改动最小，不引入无关重构。
5. 修复后运行与本轮修改对应的最小验证；验证通过后再进入下一轮 review。
6. 直到某一轮在既定目标范围内找不出需要处理的问题为止；最后输出最终验证和仍未覆盖的风险边界。

## 研究和数据原则

- 任何研究结论必须绑定数据版本、样本区间、频率、资产池、过滤条件、复权 / 合约续接方式、时区、交易日历和成本假设。
- 所有时间序列处理必须明确 timestamp 语义：事件时间、接收时间、bar close time、signal time、order time 和 fill time 不得混用。
- 因子和标签必须显式处理滞后，禁止使用未来数据。对 rolling window、groupby、join、resample、forward fill 和 label construction 特别检查 look-ahead。
- 训练、验证、测试切分默认按时间切分；涉及横截面和多资产时，明确是否存在同日泄漏、同标的泄漏或重复样本。
- 对缺失值、异常值、停牌 / 无成交、涨跌停、低流动性、极端波动和数据供应商修订，应有明确策略，不用静默填充掩盖问题。
- 新增数据集或中间表时，应说明 schema、主键、排序键、分区方式、单位、时区、去重规则和质量检查。
- 大体积数据、原始行情、模型权重和缓存默认不进 git；需要追踪时优先提交 schema、manifest、校验摘要、生成脚本和小样本 fixture。
- 性能优化先定位瓶颈，再通过 profile、benchmark 或端到端任务耗时验证收益。不要为了局部微优化破坏研究代码的可读性和可验证性。

## 回测和模型验证原则

- 回测必须明确从信号到持仓再到成交的时间顺序，不能用当根 bar 的收盘信息在同一时刻成交，除非策略假设和撮合模型明确支持。
- 交易成本、滑点、冲击成本、成交量约束、涨跌停 / 停牌 / 无流动性、最小交易单位和资金占用应按研究目标显式建模。
- 回测指标至少区分 gross / net 表现；涉及换手或高频信号时，必须报告 turnover、成本敏感性和容量风险。
- 因子验证不要只报告单个平均指标；优先同时看分期稳定性、分组单调性、decay、极端日影响、相关性和样本外表现。
- 模型评估必须记录 label 定义、特征集合、数据切分、随机种子、训练参数、评价指标和 baseline。没有 baseline 或 ablation 时，不要声称模型改进成立。
- 对调参和模型选择，避免把测试集用于反复决策；需要多轮研究时，应保留最终 holdout 或 walk-forward 评估。
- 任何 PnL、收益或模型效果结论都应说明是否已包含成本、是否样本外、是否经过参数选择、是否可复现。

## Adaptive Development 工作流

本仓库统一使用 `adaptive-development` 代替 Superpowers 开发流程。Skill 的可提交事实源位于 `docs/skills/adaptive-development/SKILL.md`；如果当前 Codex catalog 已安装该 skill，使用 `$adaptive-development`，否则直接读取仓库副本并执行相同流程。

除非用户明确要求，本仓库不调用 `superpowers:*` 工作流。其他用于特定工具、外部文档或格式处理的窄域 skill 不受此限制。

每次讨论涉及编码、实现、测试、构建或运行配置、数据处理、因子研究、回测、模型训练、性能优化、长期技术目标或反复技术迭代时，先向用户输出：

```text
Level: L0|L1|L2|L3 — <一句判断依据>. Workflow: <本轮启用的质量门>.
```

输出分类后直接执行，不询问用户分类是否正确；用户有不同意见时按用户指定调整。执行中发现影响范围、不确定性、不可逆性或后果上升时，只能向上升级 Level，并立即补齐更高级别的质量门。

### Level 摘要

- `L0`：只读解释、调查、诊断或 review；不写计划，不修改文件，不创建 branch、commit 或 PR。
- `L1`：无预期行为变化的机械修改；不写计划，做最小 diff review、最小验证和原子提交。
- `L2`：边界明确、可验证且易回滚的局部行为修改；只写会话内简短计划，优先先建立失败测试、最小复现或小样本实验，再实现、review、focused verification 和原子提交。
- `L3`：高影响、高后果、难回滚、跨模块或长期有效的设计与实现；首次修改前应使用专用 branch 或 worktree，写 markdown 计划，完成阶段 review、完整风险验证、原子提交，并按用户要求 push / PR。

### aries 的 L3 升级条件

除 skill 的通用条件外，下列任务默认归类为 `L3`：

- 修改数据 schema、数据版本规则、存储格式、分区策略或关键数据生成 contract。
- 修改因子计算框架、回测框架、成本模型、撮合假设、绩效指标口径或模型训练 / 评估主流程。
- 变更会影响既有研究结论、历史实验复现、样本切分、标签定义或模型选择。
- 声明或追求策略收益、风险指标、模型效果、容量、计算性能或存储效率提升。
- 引入长期架构、跨模块 contract、长期技术目标，或反复优化已经暴露结构性问题。

仓库已有的 build、test、benchmark、数据校验、回测 smoke 和文档事实源规则继续生效；`adaptive-development` 负责选择流程强度，不替代研究 contract。

## Grill Me 设计追问

当前约定使用两个 Grill Me skill：

- `grill-me-basic`：基础版，按每轮 5 个问题批量追问，适合任务需求讨论、轻量设计讨论、单模块方案澄清，或用户希望快速被追问但不需要生成计划审查产物时使用。
- `grill-me-enhanced`：加强版，先按每轮 5 个问题批量完成需求 / 设计追问，再把锁定后的计划交给 Codex 做 read-only adversarial review；适合跨模块架构、数据 schema、因子 / 标签定义、回测框架、成本模型、模型评估、性能取舍和其他高风险方案。

在任务需求讨论和设计方案时，代理需要根据自己的判断询问用户是否启用 `grill-me-basic` 还是 `grill-me-enhanced`，并给出推荐理由。普通需求澄清、单模块设计或低风险方案默认建议 `grill-me-basic`；涉及数据 contract、因子框架、回测假设、模型训练评估、样本切分、统计口径、性能瓶颈或长期架构取舍时默认建议 `grill-me-enhanced`。

Grill Me 只在用户确认后启用；如果用户拒绝或只想继续普通讨论，按 `adaptive-development` 分类继续，不再切换到其他通用开发流程。启用 `grill-me-enhanced` 前，应提醒用户它会产生锁定计划和 review log；这些产物应优先放在项目文档约定路径或用户指定路径，避免把临时计划文件长期散落在仓库根目录。

启用 `grill-me-basic` 或 `grill-me-enhanced` 后，追问阶段默认每轮提出 5 个当前最重要的问题；如果剩余未决问题不足 5 个，只问剩余问题。每个问题都要附上代理的建议答案或推荐取舍，用户一次性统一回答整轮问题后，代理再根据回答推进下一轮。事实性问题应优先由代理通过代码、文档或工具自行查证，不把可自行确认的事实问题丢给用户；需要用户回答的是目标、取舍、边界和风险接受度。

## 编码和依赖使用约定

- 新写或修改的项目自有代码，命名、缩进、格式化、类型注解和 include / import 排序遵循仓库现有风格；第三方代码、生成代码和外部样例优先保持上游或生成器输出风格。
- 不新增与当前任务无关的依赖。新增依赖前先确认它是否解决了真实复杂度，并说明对安装、复现、性能和长期维护的影响。
- 数据处理优先使用确定性、可复现、可测试的实现；涉及浮点、排序、并行、随机数、采样和 groupby 时，应固定随机种子并记录排序 / 聚合语义。
- 低层工具函数应配小样本 fixture 或单元测试；数据 pipeline、因子计算、回测和模型训练入口应提供最小 smoke 或 dry-run 验证方式。
- Notebook 适合探索和报告，不应成为唯一事实源。可复用逻辑应迁移到脚本、模块或 pipeline，并保留可命令行复现的入口。
- 打印输出、日志和报告字段应稳定命名；新增、重命名或删除关键指标、诊断字段、report 字段时，同步更新对应文档或 README。

## 项目级执行规则

- 修改数据读取、清洗、join、resample、bar 构造、因子计算、标签定义、回测或模型评估时，优先补充或更新测试、fixture、小样本复现或 smoke。
- 涉及时间顺序、样本切分、缺失值、去重、复权、合约续接、成本模型、成交假设或指标口径的变更，必须显式验证结果。
- 做研究实验时，先记录实验目的、数据范围、参数、随机种子和验证命令；完成后记录关键结果和边界，不把单次偶然结果写成稳定结论。
- 做性能优化时，先用 profile、benchmark 或明确任务耗时锁定瓶颈，再做最小改动；完成后重新跑对应验证，确认收益真实存在且没有行为回归。
- 修改应尽量最小化，避免顺手引入无关重构。
- 如果工作区已有未提交更改，先读取并理解现状，再在现有基础上做最小修改。不要 revert、reset 或覆盖自己没有创建的改动，除非用户明确要求。
- 提交应保持原子性：代码实现、文档同步、实验结果、benchmark 或额外整理如果不是同一项最小闭环，优先分开提交。提交前至少运行 `git diff --check` 和与变更相关的最小验证。
- 除非用户明确要求，不主动 push。

## 常用验证命令

```bash
git status --short --branch
git log --oneline -8
git diff --check
```

项目具体构建、测试、数据校验、回测 smoke、benchmark 和模型训练命令以 README、docs 和当前代码为准。若文档缺失，先从现有脚本、配置和测试入口中确认，不凭记忆编造命令。

## 一句话原则

先确认数据和时间边界，再研究；先验证口径和泄漏风险，再相信指标；先跑可复现实验，再宣称结论。
